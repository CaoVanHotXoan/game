{
  "entities": {
    "Score": {
      "title": "Score",
      "description": "A player's game score and survival time.",
      "type": "object",
      "properties": {
        "username": {
          "type": "string",
          "description": "The name of the player."
        },
        "score": {
          "type": "integer",
          "description": "The score achieved in the game."
        },
        "survival_time": {
          "type": "integer",
          "description": "The time the player survived in seconds."
        },
        "created_at": {
          "type": "string",
          "format": "date-time",
          "description": "When the score was recorded."
        }
      },
      "required": ["username", "score", "survival_time", "created_at"]
    }
  },
  "firestore": {
    "/scores/{scoreId}": {
      "schema": "Score",
      "description": "Collection of game scores for the leaderboard."
    }
  }
}
